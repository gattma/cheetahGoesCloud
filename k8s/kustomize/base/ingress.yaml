apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cheetah-goes-cloud-kustomize
spec:
  ingressClassName: nginx
  rules:
    - host: cheetah-goes-cloud-kustomize.localdev.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cheetah-goes-cloud-kustomize
                port:
                  number: 8080
