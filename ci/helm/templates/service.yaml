apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo_microservice.fullname" . }}
  labels:
    {{- include "demo_microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo_microservice.selectorLabels" . | nindent 4 }}
